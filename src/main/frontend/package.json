{
  "name": "frontend",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i ./main.css -o ../resources/static/app.css",
    "watch": "tailwindcss -i ./main.css -o ../resources/static/app.css --watch --no-clear"
  },
  "devDependencies": {
    "tailwindcss": "^3.2.2"
  }
}
